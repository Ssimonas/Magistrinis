1. Read all files from a folder? - specify a year interval as parameter
2. Check that all variables from all files do match
3. Remove all similiar, not needed variables from datasets?
4. Make sure that formats are right for all variables